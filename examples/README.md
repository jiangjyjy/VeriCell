# Offline example

The supported example is the repository-root command:

```bash
python run_demo.py --config configs/default.yaml --output outputs/demo
```

It is a deterministic synthetic regression demonstration.  It is intentionally
separate from the manuscript's real benchmark experiments and must not be
interpreted as a regenerated Table 1--10 result.
